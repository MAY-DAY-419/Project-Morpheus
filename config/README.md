# Config & Deployment ⚙️

- Store authority public keys and versions in a config endpoint.
- Environment variables for DB and server secrets (but not report keys).
- Frontend can be deployed to Vercel; backend behind Nginx/Apache or cloud host.
