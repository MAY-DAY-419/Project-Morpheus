# Backend — Notes 🚦

Tech: Go (Golang), RESTful JSON APIs

Responsibilities
- Accept encrypted payloads and store them.
- Serve encrypted blobs to authorized authority clients.
- Provide authority registration (public keys) and key versioning endpoints.

Important: backend NEVER receives plaintext or per-report keys.
